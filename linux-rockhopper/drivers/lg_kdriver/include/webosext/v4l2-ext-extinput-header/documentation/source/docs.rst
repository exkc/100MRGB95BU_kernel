Documents
=========

Documents are some descriptions across two or more modules (something like the theme).
For example, a document about decoder refers about both audio decoder and video decoder.
Two modules are existed independently,
but there may be common decoder information about audio and video.

.. toctree::
  :maxdepth: 1
  :glob:

  docs/*
